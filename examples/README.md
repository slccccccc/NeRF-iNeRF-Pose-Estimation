# Examples

The repository does not redistribute image datasets. Put calibration images
and correspondence files in this directory only for local experiments.

`object_points.csv` must contain three columns (`x,y,z`) and
`image_points.csv` must contain two columns (`u,v`). Rows must describe the
same physical points in the same order.
